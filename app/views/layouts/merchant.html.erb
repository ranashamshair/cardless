<!DOCTYPE html>
<html>
  <head>
    <title>Cardless</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    
    <%= stylesheet_link_tag 'merchant', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <!-- Google Fonts -->
    <link rel="stylesheet"
        href="https://fonts.googleapis.com/css2?family=Red+Hat+Display:wght@400;500;700&display=swap">

    <!-- Font Awesome -->
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/5.15.2/css/all.min.css">
  </head>

  <body>
      <div class="dash_body" >
        <%= render partial: "shared/merchant/left_sidebar" %>
        <%= render partial: "shared/merchant/right_sidebar" %>
        <%= render partial: "shared/merchant/notification_sidebar"%>
        <%= yield %>
      </div>
  </body>
</html>
